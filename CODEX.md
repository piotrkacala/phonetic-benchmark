# CODEX.md

Read `AGENTS.md` first.
It is the canonical instruction file for this repository.

Then read `agent-collaboration-defaults.md`.

Codex-specific note:
- Prefer repository docs and benchmark data over assumptions from any previous React shell.
- If the current task is an implementation run, add the required application code instead of
  treating the docs-only baseline as a prohibition on implementation.
